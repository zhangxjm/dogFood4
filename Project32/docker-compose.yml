version: '3.8'

services:
  campus-job:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: campus-job
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    environment:
      - GO_ENV=production
