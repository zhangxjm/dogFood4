version: '3.8'

services:
  backend:
    build: ./backend
    container_name: petshop-backend
    ports:
      - "8080:8080"
    volumes:
      - ./backend:/app
      - petshop-data:/app/data

volumes:
  petshop-data:
