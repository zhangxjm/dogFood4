version: '3.8'

services:
  mongodb:
    image: mongo:7.0
    container_name: account-book-mongo
    restart: always
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
