server:
  port: 8080

spring:
  application:
    name: gym-reservation
  datasource:
    url: jdbc:mysql://localhost:3307/gym_reservation?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: gym
    password: gym123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

logging:
  level:
    com.gym.reservation: DEBUG
    org.hibernate.SQL: DEBUG
