server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/meeting_room?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: meeting
    password: meeting123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect

jwt:
  secret: meeting-room-secret-key-2024-very-long-secret
  expiration: 86400000

logging:
  level:
    com.meeting: DEBUG
