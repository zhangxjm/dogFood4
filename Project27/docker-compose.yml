services:
  mongodb:
    image: mongo:6
    container_name: wishlist-mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
    environment:
      MONGO_INITDB_DATABASE: wishlist

volumes:
  mongodb_data:
